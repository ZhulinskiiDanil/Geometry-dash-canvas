const initSize = window.innerWidth / 100 + window.innerHeight / 100

const settings = {
  collision: false,
  speed: initSize * .4, // initSize * .4
  cube: {
    skin: 2 // 1
  },
  ship: {
    skin: 1
  }
}

export default settings